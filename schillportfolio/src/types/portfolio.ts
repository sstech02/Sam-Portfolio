export type StackItem = {
  name: string
  cat: string
  icon: string
}

export type Project = {
  id: string
  title: string
  year: string
  cat: string
  desc: string
  tags: string[]
  url: string
  bg: string
}
